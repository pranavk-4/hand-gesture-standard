# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# Data — regenerated by scripts/run_baseline.py, never committed
data/raw/
data/processed/

# Weights + large binaries — metrics/plots are the evidence, checkpoints are not
outputs/*/checkpoints/
outputs/**/*.pth
outputs/**/*.onnx
outputs/**/*.pt

# HPO trial DBs can grow large; keep trials.csv, ignore sqlite WAL
outputs/hpo/*.db*
*.db-wal
*.db-shm

# Scratch
.superpowers/
